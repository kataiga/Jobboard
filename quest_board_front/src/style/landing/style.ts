import React from "react";
import COLORS from "../colors";

export const marginTop:React.CSSProperties = {
    marginTop:"6%",
    color:COLORS.PURPLE
}

export const landingText:React.CSSProperties = {
    color:COLORS.PURPLE,
    fontSize:100
}

export const marginBottom:React.CSSProperties = {
    marginTop:"18%",
}

export const linkColor:React.CSSProperties = {
    color:COLORS.DANGER,
    textDecoration:'none',
    cursor: "pointer",
}

export const fontSize:React.CSSProperties = {
    fontSize:25,
}

export const background:React.CSSProperties = {
    backgroundImage:"url('/images/background.png')",
    backgroundPosition:"center",
    backgroundSize:"cover",
    backgroundRepeat: 'no-repeat',
}
