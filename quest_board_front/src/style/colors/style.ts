import React from "react";
import COLORS from "../colors";

export const primary:React.CSSProperties = {
    backgroundColor:COLORS.PRIMARY
}

export const danger:React.CSSProperties = {
    backgroundColor:COLORS.DANGER
}

export const secondary:React.CSSProperties = {
    backgroundColor:COLORS.SECONDARY
}

export const basic:React.CSSProperties = {
    backgroundColor:COLORS.BASIC
}

export const purple:React.CSSProperties = {
    backgroundColor:COLORS.PURPLE
}

export const success:React.CSSProperties = {
    backgroundColor:COLORS.SUCCESS
}

export const whiteText:React.CSSProperties = {
    color:COLORS.BASIC
}

export const hidenText:React.CSSProperties = {
    color:"#E47171"
}

export const whiteBorder:React.CSSProperties = {
    borderColor:COLORS.BASIC
}